# Keep legacy pip install support; primary deps moved to pyproject.toml / uv.lock.
moviepy==2.1.2
streamlit==1.45.0
edge_tts==7.2.7
fastapi==0.115.6
uvicorn==0.32.1
openai==1.56.1
faster-whisper==1.1.0
loguru==0.7.3
google.generativeai==0.8.6
dashscope==1.20.14
g4f==0.5.2.2
azure-cognitiveservices-speech==1.41.1
redis==5.2.0
python-multipart==0.0.19
pyyaml==6.0.3
requests==2.33.1
socksio==1.0.0
pydub==0.25.1
litellm==1.60.0

# Local Silero TTS (RU) runs on PyTorch. The CPU build comes from the PyTorch
# index below (keeps the download small). The v5_ru model (~140MB) is fetched
# on first use and cached under storage/models/silero/.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.12.0
numpy==2.4.4
scipy==1.17.1
